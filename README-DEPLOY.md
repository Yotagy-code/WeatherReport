# Weather App - Инструкция по развертыванию

## 📋 Содержание
- [Локальное развертывание с Docker](#локальное-развертывание-с-docker)
- [Развертывание на GitHub Pages](#развертывание-на-github-pages)
- [Развертывание на облачных платформах](#развертывание-на-облачных-платформах)

---

## 🐳 Локальное развертывание с Docker

### Предварительные требования
- Docker установлен ([Скачать Docker](https://www.docker.com/get-started))
- Docker Compose установлен

### Быстрый старт

1. **Убедитесь, что у вас есть все файлы:**
   - `index.html`
   - `style.css`
   - `script.js`
   - `weatherapplogo.png`
   - `Dockerfile`
   - `docker-compose.yml`
   - `nginx.conf`
   - `.dockerignore`

2. **Запустите приложение**
```bash
docker-compose up -d
```

3. **Откройте в браузере**
```
http://localhost:8080
```

### Полезные команды
```bash
# Посмотреть логи
docker-compose logs -f

# Остановить приложение
docker-compose down

# Перезапустить после изменений
docker-compose down
docker-compose up -d --build

# Посмотреть запущенные контейнеры
docker ps

# Зайти внутрь контейнера
docker exec -it weather-app sh
```

---

## 🌐 Развертывание на GitHub Pages

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **"New repository"**
3. Название: `weather-app`
4. Выберите **Public**
5. Нажмите **"Create repository"**

### Шаг 2: Загрузите файлы

1. Нажмите **"uploading an existing file"**
2. Загрузите все файлы:
   - `index.html`
   - `style.css`
   - `script.js`
   - `weatherapplogo.png`
3. Нажмите **"Commit changes"**

### Шаг 3: Включите GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. В **"Source"** выберите: `main` и `/ (root)`
3. Нажмите **"Save"**
4. Подождите 2 минуты

### Шаг 4: Готово! 🎉

Ваш сайт будет доступен по адресу:
```
https://ваш-username.github.io/weather-app/
```

---

## ☁️ Развертывание на облачных платформах

### Vercel (РЕКОМЕНДУЕТСЯ)

1. **Зарегистрируйтесь на Vercel**
   - Зайдите на https://vercel.com
   - Нажмите **"Sign Up"**
   - Войдите через GitHub

2. **Создайте проект**
   - Нажмите **"Add New Project"**
   - Выберите ваш репозиторий `weather-app`
   - Нажмите **"Deploy"**

3. **Готово!**
```
   https://weather-app-xyz.vercel.app
```

### Netlify

1. **Зарегистрируйтесь на Netlify**
   - Зайдите на https://www.netlify.com
   - Нажмите **"Sign up"**

2. **Два способа развертывания:**

   **Способ А: Drag & Drop (Самый простой)**
   - Перейдите на https://app.netlify.com/drop
   - Перетащите папку с файлами
   - Готово!

   **Способ Б: Через GitHub**
   - Нажмите **"Add new site"** → **"Import an existing project"**
   - Выберите GitHub
   - Выберите репозиторий `weather-app`
   - Нажмите **"Deploy"**

3. **Готово!**
```
   https://weather-app-xyz.netlify.app
```

### Railway.app

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Выберите **"Deploy from GitHub repo"**
4. Выберите `weather-app`
5. Railway автоматически обнаружит Dockerfile
6. Нажмите **"Deploy"**

### Render.com

1. Зарегистрируйтесь на https://render.com
2. Нажмите **"New Static Site"**
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: (оставьте пустым)
   - Publish Directory: `.`
5. Нажмите **"Create Static Site"**

---

## 🖥️ Развертывание на VPS

### Шаг 1: Подготовка сервера (Ubuntu/Debian)
```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установить Docker Compose
sudo apt install docker-compose -y

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
```

### Шаг 2: Загрузка файлов на сервер
```bash
# Через Git
git clone https://github.com/your-username/weather-app.git
cd weather-app

# Или через SCP
scp -r ./* user@your-server-ip:/home/user/weather-app/
```

### Шаг 3: Запуск приложения
```bash
cd weather-app
docker-compose up -d
```

Приложение доступно на `http://your-server-ip:8080`

### Шаг 4: Настройка домена (опционально)

**Установите Nginx:**
```bash
sudo apt install nginx -y
```

**Создайте конфигурацию:**
```bash
sudo nano /etc/nginx/sites-available/weather-app
```

**Добавьте:**
```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Активируйте:**
```bash
sudo ln -s /etc/nginx/sites-available/weather-app /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### Шаг 5: SSL сертификат
```bash
# Установить Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получить SSL сертификат
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

---

## 🔧 Настройка API ключа

**⚠️ ВАЖНО:** В файле `script.js` есть API ключ OpenWeatherMap.

Если хотите использовать свой ключ:

1. Зарегистрируйтесь на https://openweathermap.org/api
2. Получите бесплатный API ключ
3. Замените в `script.js`:
```javascript
   const API_KEY = 'ВАШ_API_КЛЮЧ';
```

---

## 🆘 Решение проблем

### Проблема 1: Сайт не загружает погоду
- Проверьте что API ключ действителен
- Откройте Developer Tools (F12) → Console
- Проверьте наличие ошибок

### Проблема 2: Порт уже занят
```bash
# Узнать что использует порт 8080
sudo lsof -i :8080

# Или измените порт в docker-compose.yml на другой
ports:
  - "3000:80"
```

### Проблема 3: Docker не запускается
```bash
# Проверить логи
docker-compose logs

# Пересобрать образ
docker-compose up --build
```

---

## 📱 Особенности приложения

- ✅ Автоматическое определение местоположения
- ✅ Прогноз на 5 дней
- ✅ Поиск города
- ✅ Адаптивный дизайн
- ✅ Красивая анимация
- ✅ Сохранение последнего города

---

## 🚀 Production Checklist

- [ ] API ключ получен и работает
- [ ] SSL сертификат установлен (для домена)
- [ ] Домен настроен (если есть)
- [ ] Gzip сжатие включено (уже в nginx.conf)
- [ ] Кеширование настроено (уже в nginx.conf)
- [ ] Приложение протестировано на разных устройствах

---

## 🎯 Какой вариант выбрать?

| Вариант | Сложность | Цена | Время | Лучше для |
|---------|-----------|------|-------|-----------|
| **Netlify Drop** | 😊 Очень легко | Бесплатно | 2 мин | Быстрый тест |
| **GitHub Pages** | 😊 Легко | Бесплатно | 5 мин | Портфолио |
| **Vercel** | 😊 Легко | Бесплатно | 5 мин | ⭐ Лучший выбор |
| **Railway** | 😐 Средне | $5/мес | 10 мин | Docker проекты |
| **VPS** | 😅 Сложно | От $4/мес | 1 час | Полный контроль |

**Рекомендация:** Начните с Vercel или Netlify Drop! 🚀

---

Готово! 🎉
